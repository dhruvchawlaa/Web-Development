function whosPaying(names) {
  /******Don't change the code above*******/

  //Write your code here.
  var index = Math.floor(Math.random() * names.length);
  var value = names[index] + " is going to buy lunch today!";
  return value;

  /******Don't change the code below*******/
}
